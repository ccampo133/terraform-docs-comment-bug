# terraform-docs-comment-bug

To reproduce the bug, run:

```
terraform-docs .
```

You will see the following output:

```
Error: value of '--output-template' is missing end comment
```

Last tested w/ version `v0.16.0`.